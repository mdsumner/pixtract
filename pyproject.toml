[project]
name = "pixtract"
version = "0.0.1"
description = "Planned raster value extraction: points and grouped cells via block-ordered reads"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "Michael Sumner" },
]
dependencies = [
    "numpy",
    #"gdal", osgeo.gdal is required but we don't want to build it or install from binary (WIP)
]

[project.optional-dependencies]
rio = ["rasterio"]
dev = ["pytest", "rasterio"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["python/pixtract"]

[tool.pytest.ini_options]
testpaths = ["tests"]
